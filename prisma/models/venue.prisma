model Venue {
  id       String @id @default(uuid()) @db.VarChar(36)
  name     String
  province String
  address  String
  mapUrl   String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  events Event[]
}