model Category {
  id           String  @id @default(uuid()) @db.VarChar(36)
  name         String  @unique
  iconUrl      String?
  iconPublicId String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  events Event[]
}